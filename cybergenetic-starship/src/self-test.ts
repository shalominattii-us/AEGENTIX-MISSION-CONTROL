import {strict as assert} from 'node:assert';import {createStarship} from './starship.js';
const s=createStarship();const e=s.ableton.midi('NOTE_ON',{note:'C4',velocity:90});assert.equal(e.decision,'APPROVED');assert.equal(s.runtime.verify(e),true);const c=s.robotics.command('ROBOT-01','MOVE',{x:1});assert.equal(c.decision,'ESCALATE');const a=[...s.runtime.approvals.values()][0];assert.ok(a);s.runtime.decide(a.id,'APPROVED','OPERATOR');s.campus.add({id:'CAMPUS-01',name:'Main',region:'US',online:true,students:100});assert.equal(s.recorder.verify(),true);assert.equal(s.moe.route('create an Ableton harmony lesson').selected[0].name,'MusicExpert');console.log('CYBERGENETIC STARSHIP SELF-TEST PASS');
